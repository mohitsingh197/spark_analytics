object test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(57); 
  println("Welcome to the Scala worksheet");$skip(46); 
  
  val Array(name, id) = new Int[](100, 01);System.out.println("""name  : <error> = """ + $show(name ));System.out.println("""id  : <error> = """ + $show(id ))}
  
  }
